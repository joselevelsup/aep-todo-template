//Always remember that all hooks in react start with "use"
import { useQuery, useMutation } from "@tanstack/react-query";

export default function Index() {
  return (
    <div className="text-center">This is where all the todos will live</div>
  )
}
